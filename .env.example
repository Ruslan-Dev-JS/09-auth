# Base URL for metadata and Open Graph (optional).
# On Vercel the URL is detected automatically via VERCEL_URL.
# For local dev with production URL in meta tags, set:
NEXT_PUBLIC_APP_URL=https://08-zustand-three-fawn.vercel.app
